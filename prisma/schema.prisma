datasource db {
  provider = "mongodb"
  url      = env("mongodb://atlas-sql-68908cef5ef99a38c3bd815c-om4iee.a.query.mongodb.net/myVirtualDatabase?ssl=true&authSource=admin")
}

generator client {
  provider = "prisma-client-js"
}

model Contact {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  email     String
  subject   String
  message   String
  createdAt DateTime @default(now())
}