datasource db {
  provider = "mongodb"
  url      = env(mongodb+srv://Aditya_tiwari:Adiksha@123@cluster0.4rr7imt.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0)
}

generator client {
  provider = "prisma-client-js"
}

model Contact {
  id        String   @id @default(auto()) @map("_id")
  name      String
  email     String
  subject   String
  message   String
  createdAt DateTime @default(now())
}